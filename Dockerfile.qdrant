FROM qdrant/qdrant:v1.9.7

# qdrant:v1.9.7 does not ship wget or curl.
# Use bash's built-in /dev/tcp to probe the REST port — no external tools needed.
HEALTHCHECK --interval=10s --timeout=5s --start-period=30s --retries=5 \
  CMD bash -c 'exec 3<>/dev/tcp/127.0.0.1/6333' || exit 1
