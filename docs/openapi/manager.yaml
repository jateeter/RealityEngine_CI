openapi: 3.1.0
info:
  title: RealityEngine Manager API (external)
  version: 1.1.0
  description: Generated from SURFACE_SPEC.md — do not edit by hand.
  x-generated-from: RealityEngine_CI/SURFACE_SPEC.md
servers: []
tags:
- name: Configuration
- name: Engines
paths:
  /api/engines:
    get:
      tags:
      - Engines
      summary: List the registered engine instances
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Object'
        '500':
          $ref: '#/components/responses/InternalError'
      operationId: get_api_engines
  /api/engine/{id}/health:
    get:
      tags:
      - Engines
      summary: Health of one engine, probed
      parameters:
      - &id001
        name: id
        in: path
        required: true
        schema:
          type: string
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Health'
        '500':
          $ref: '#/components/responses/InternalError'
        '404':
          $ref: '#/components/responses/NotFound'
      operationId: get_api_engine_id_health
  /api/engine/{id}/vectors/{vectorId}:
    get:
      tags:
      - Engines
      summary: Read a vector from the named engine
      parameters:
      - *id001
      - name: vectorId
        in: path
        required: true
        schema:
          type: string
        description: Scoped to the engine named by {id}
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Object'
        '500':
          $ref: '#/components/responses/InternalError'
        '404':
          $ref: '#/components/responses/NotFound'
      operationId: get_api_engine_id_vectors_vectorId
  /api/engine/{id}/sequences/{sequenceId}:
    get:
      tags:
      - Engines
      summary: Read a sequence from the named engine
      parameters:
      - *id001
      - name: sequenceId
        in: path
        required: true
        schema:
          type: string
        description: Scoped to the engine named by {id}
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Object'
        '500':
          $ref: '#/components/responses/InternalError'
        '404':
          $ref: '#/components/responses/NotFound'
      operationId: get_api_engine_id_sequences_sequenceId
  /api/engine/{id}/config:
    get:
      tags:
      - Configuration
      summary: Every control on the named engine
      parameters:
      - *id001
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/EngineConfig'
        '500':
          $ref: '#/components/responses/InternalError'
        '404':
          $ref: '#/components/responses/NotFound'
      operationId: get_api_engine_id_config
  /api/engine/{id}/config/{control}:
    get:
      tags:
      - Configuration
      summary: Read one control on the named engine
      parameters:
      - *id001
      - &id002
        name: control
        in: path
        required: true
        schema:
          type: string
        description: Control name, as declared in SURFACE_SPEC
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: object
                properties:
                  control:
                    $ref: '#/components/schemas/EngineControl'
        '500':
          $ref: '#/components/responses/InternalError'
        '404':
          $ref: '#/components/responses/NotFound'
      operationId: get_api_engine_id_config_control
    put:
      tags:
      - Configuration
      summary: Set one control on the named engine
      parameters:
      - *id001
      - *id002
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/EngineControlWrite'
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: object
                properties:
                  control:
                    $ref: '#/components/schemas/EngineControl'
        '500':
          $ref: '#/components/responses/InternalError'
        '404':
          $ref: '#/components/responses/NotFound'
      operationId: put_api_engine_id_config_control
    delete:
      tags:
      - Configuration
      summary: Restore one control on the named engine to its declared default
      parameters:
      - *id001
      - *id002
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: object
                properties:
                  control:
                    $ref: '#/components/schemas/EngineControl'
        '500':
          $ref: '#/components/responses/InternalError'
        '404':
          $ref: '#/components/responses/NotFound'
      operationId: delete_api_engine_id_config_control
components:
  parameters:
    MachineId:
      name: id
      in: path
      required: true
      schema:
        type: string
  responses:
    NotFound:
      description: Resource not found
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/Error'
    InternalError:
      description: Internal server error
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/Error'
  schemas:
    Object:
      type: object
      additionalProperties: true
    Vector:
      type: array
      items:
        type: number
        format: double
    Success:
      type: object
      properties:
        success:
          type: boolean
    Error:
      type: object
      properties:
        error:
          type: string
    Health:
      type: object
      properties:
        status:
          type: string
          example: healthy
        timestamp:
          type: number
        version:
          type: string
    RealityConfig:
      type: object
      properties:
        eventDimension:
          type: integer
          example: 7680
        matchThreshold:
          type: number
          example: 0.5
        qdrantUrl:
          type: string
        collectionName:
          type: string
          example: reality-events
    EngineControl:
      type: object
      required:
      - name
      - scope
      - value
      - default
      - mutable
      properties:
        name:
          type: string
          example: transitionsInhibited
        scope:
          type: string
          enum:
          - engine
          - machine
        value:
          description: Scalar when scope is engine; an object keyed by machine id when scope is machine
        default:
          description: The default this document declares
        mutable:
          type: boolean
          description: Whether PUT is accepted; a derived reading is reported, not set
    EngineConfig:
      type: object
      properties:
        controls:
          type: array
          items:
            $ref: '#/components/schemas/EngineControl'
          description: Emitted sorted by name
    EngineControlWrite:
      type: object
      required:
      - value
      properties:
        machine:
          type: string
          description: Required when the control's scope is machine
        value:
          description: The value to set
    RuntimeOptions:
      type: object
      properties:
        historyLimit:
          type: integer
          example: 250
        includeMachineResults:
          type: boolean
        includePerceptualSpace:
          type: boolean
    RuntimeOptionsPatch:
      type: object
      properties:
        historyLimit:
          type: integer
          minimum: 0
        includeMachineResults:
          type: boolean
        includePerceptualSpace:
          type: boolean
    Region:
      type: object
      required:
      - offset
      - length
      properties:
        offset:
          type: integer
        length:
          type: integer
    Machine:
      type: object
      additionalProperties: true
      description: RealityEngine machine JSON.
      properties:
        id:
          type: string
        name:
          type: string
        version:
          type: string
        perceptualMapping:
          type: object
          additionalProperties: true
        sequences:
          type: array
          items:
            type: object
            additionalProperties: true
    MachineEnvelope:
      type: object
      required:
      - version
      - machine
      properties:
        version:
          type: string
          description: Corpus file format version. Required in this shape and validated on its major component; the bare Machine
            schema does not declare it.
        machine:
          $ref: '#/components/schemas/Machine'
    MachineMutationResponse:
      type: object
      properties:
        success:
          type: boolean
        machine:
          $ref: '#/components/schemas/Machine'
        message:
          type: string
    MachineTransitionResult:
      type: object
      additionalProperties: true
      properties:
        inputEvent:
          $ref: '#/components/schemas/Vector'
        timestamp:
          type: number
        machineOutput:
          type:
          - array
          - 'null'
          items:
            type: number
            format: double
        sequenceResults:
          type: array
          items:
            type: object
            additionalProperties: true
    SimulationStep:
      type: object
      properties:
        stepNumber:
          type: integer
        timestamp:
          type: number
        perceptualSpace:
          $ref: '#/components/schemas/Vector'
        machineResults:
          type: object
          additionalProperties: true
    SimulationConfigureChunk:
      type: object
      properties:
        reset:
          type: boolean
        vectors:
          type: array
          items:
            $ref: '#/components/schemas/Vector'
        config:
          type: object
          additionalProperties: true
    PerceiveRequest:
      type: object
      required:
      - vector
      properties:
        vector:
          $ref: '#/components/schemas/Vector'
        compact:
          type: boolean
        includeMachineResults:
          type: boolean
        includePerceptualSpace:
          type: boolean
    PagingDecision:
      type: object
      properties:
        machineId:
          type: string
        sequenceId:
          type: string
        ragStatusCode:
          type: string
          enum:
          - GREEN
          - AMBER
          - RED
        ownerTeam:
          type: string
        runbook:
          type: string
        source:
          type: string
          enum:
          - rule-with-override
          - rule-only
          - machine-fallback
